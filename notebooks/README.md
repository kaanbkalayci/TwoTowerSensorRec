# Notebooks

Use notebooks for exploration and narrative analysis.

Suggested naming:

```text
YYYY-MM-DD_short-description.ipynb
```

When notebook logic becomes reusable, move it into `src/` or `scripts/`.

